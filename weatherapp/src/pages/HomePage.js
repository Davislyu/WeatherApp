import React from 'react';
import Weather from "../features/weather/Weather";

const HomePage = () => {
    return (
        <div>
            <Weather />
        </div>
    );
};

export default HomePage;
